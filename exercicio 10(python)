# Declaração de variáveis
Fah = float(input("Me diga uma temperatura em Fahrenheit: "))

# Cálculo da conversão
Cel = (Fah - 32) * 5 / 9

# Saída do resultado
print("Convertendo Fahrenheit para Celsius fica:", Cel)
